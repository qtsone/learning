package sq

// Balanced reports whether every bracket in s — (), [], {} — is closed in
// the right order. Non-bracket characters are ignored.
func Balanced(s string) bool {
	// TODO: scan s rune by rune with your Stack: push openers; on a closer,
	// pop and check that it pairs up. See the pseudocode in LESSON.md.
	return false
}
