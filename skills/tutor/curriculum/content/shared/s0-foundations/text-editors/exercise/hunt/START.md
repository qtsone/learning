# Treasure hunt

Rules: hands off the file tree and the mouse. Every jump happens through
quick open — `Cmd+P` (macOS) / `Ctrl+P` (Linux) — by typing a few letters of
the next file's name. Notice you don't need the full name or the folder:
fuzzy matching does the work.

First clue: the trail begins at a place called **quiet-harbor**.
