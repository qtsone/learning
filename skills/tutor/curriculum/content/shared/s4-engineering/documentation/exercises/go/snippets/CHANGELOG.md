# Changelog

All notable, user-visible changes to this project are recorded here, newest
first. The format follows [Keep a Changelog](https://keepachangelog.com/);
version numbers follow semantic versioning (pre-1.0: minor bumps may break).

## [0.2.0] — unreleased

(exercise part 4 — write this entry from `HISTORY.txt`)

## [0.1.0] — 2026-05-14

### Added

- `add` command: store a named text snippet.
- `list` command: print the names of every stored snippet.
- `-dir` flag: choose the directory the snippet file is kept in.
