# Local-only settings. Never belongs in an image: everything the build context
# carries can end up in a layer, and layers are shipped to whoever pulls them.
CLOCKWORK_ADDR=:8080
CLOCKWORK_API_TOKEN=replace-me-locally
