# Reference .env. On a learner's machine this file is created from
# .env.example and never committed — it is per-machine, and it holds secrets.
POSTGRES_PASSWORD=local-dev-only-password
APP_PORT=8080
